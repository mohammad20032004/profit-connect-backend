info:
  name: Get Jobs
  type: http
  seq: 23

http:
  method: GET
  url: http://localhost:5000/api/jobs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
