info:
  name: Get Jobs With filter
  type: http
  seq: 22

http:
  method: GET
  url: http://localhost:5000/api/jobs?workPlace=Remote
  params:
    - name: workPlace
      value: Remote
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
