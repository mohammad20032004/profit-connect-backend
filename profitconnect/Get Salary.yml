info:
  name: Get Salary
  type: http
  seq: 26

http:
  method: GET
  url: http://localhost:5000/api/salaries?title=Frontend Developer
  params:
    - name: title
      value: Frontend Developer
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
